/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { NewsSubmissionComponent } from './news-submission.component';

describe('NewsSubmissionComponent', () => {
  let component: NewsSubmissionComponent;
  let fixture: ComponentFixture<NewsSubmissionComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [NewsSubmissionComponent]
    })
    .compileComponents();
    
    fixture = TestBed.createComponent(NewsSubmissionComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
