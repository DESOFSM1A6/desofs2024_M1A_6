import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PercursoComponent } from './percurso.component';

/*describe('PercursoComponent', () => {
  let component: PercursoComponent;
  let fixture: ComponentFixture<PercursoComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ PercursoComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(PercursoComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
